*.beam
*.ez
/build
erl_crash.dump

# lustre_ssg's generated output.
/dist

# Fetched fresh from glemy at build time (see .github/workflows/deploy.yml)
# -- not the source of truth, glemy's own docs/decisions.jsonl and
# docs/development-plan.jsonl are.
/decisions.jsonl
/development-plan.jsonl

# glemy's own build output (both the stable and edge channels -- RM-025),
# copied in at build time -- not source, a generated artifact of a
# different repo. build.gleam renames each unhashed play-demo-{stable,
# edge}/ to a content-hashed play-demo-{stable,edge}-<hash>/ in place
# (RM-024) -- the glob covers every pre- and post-rename name.
/static/play-demo*
