*.beam
*.ez
/build
erl_crash.dump

# lustre_ssg's generated output.
/dist

# Fetched fresh from glemy at build time (see .github/workflows/deploy.yml)
# -- not the source of truth, glemy's own docs/decisions.jsonl and
# docs/development-plan.jsonl are.
/decisions.jsonl
/development-plan.jsonl

# glemy's own build output, copied in at build time -- not source, a
# generated artifact of a different repo. build.gleam renames the
# unhashed play-demo/ to a content-hashed play-demo-<hash>/ in place
# (RM-024) -- the glob covers both the pre- and post-rename name.
/static/play-demo*
